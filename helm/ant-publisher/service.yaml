apiVersion: v1
kind: Service
metadata:
  name: {{ template "ant-publisher.fullname" . }}
  labels:
    app: {{ template "ant-publisher.name" . }}
    chart: {{ template "ant-publisher.chart" . }}
    release: {{ .Release.Namee }}
    heritage: {{ .Release.Service }}
spec:
  selector:
    app: {{ template "ant-publisher.name" . }}
    release: {{ .Release.Name }}
  ports:
    - name: "input"
      port: 6080
      targetPort: 6080
      protocol: TCP
  type: LoadBalancer